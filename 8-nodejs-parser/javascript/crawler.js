// Load local books
function parseBooks() {
    var newContent = `<h1>Cralwer</h1>` + 
                     `<div>URL INPUT</div>` + 
                     `<div>Button Parse</div>`;
    document.getElementById("main-content").innerHTML = newContent;
}